# Basic app nestJS with Docker and GitHub Actions

This repository contains a basic NestJS application configured to run with Docker and GitHub Actions for continuous integration and deployment. The application is set up to be built and run in a Docker container, and it includes a Makefile for convenience.

## Overview

This project is a simple setup

- Create a NestJS application.
- Expose the application on port 3000.
